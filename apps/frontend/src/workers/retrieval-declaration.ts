/**
 * Re-export the knowledge-base retrieval service declaration as a default
 * export, for the frontend host worker's dynamic import.
 */
export { declaration as default } from '@darkling/knowledge-base/service';
