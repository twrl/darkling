/**
 * Re-export the state manager authority service declaration as a default
 * export, for the frontend host worker's dynamic import.
 */
export { declaration as default } from '@darkling/state-manager/service';
